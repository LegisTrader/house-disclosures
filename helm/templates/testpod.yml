apiVersion: v1
kind: Pod
metadata:
  name: kubectl-psql-pod
  namespace: {{ .Values.namespace.name }}
spec:
  containers:
  - name: kubectl-psql
    image: postgres
    command:
    - sleep
    - "3600"
